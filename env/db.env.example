# =============================================================================
# Database
# =============================================================================
DB_HOST="db"
DB_PORT="5432"
DB_NAME="db"
DB_USER="admin"
DB_PASSWORD="admin"

POSTGRES_HOST="${DB_HOST}"
POSTGRES_PORT="${DB_PORT}"
POSTGRES_DB="${DB_NAME}"
POSTGRES_USER="${DB_USER}"
POSTGRES_PASSWORD="${DB_PASSWORD}"

DB_PING_DELAY="100"
